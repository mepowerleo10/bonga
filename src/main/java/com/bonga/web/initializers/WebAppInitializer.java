package com.bonga.web.initializers;

// import javax.servlet.ServletContext;
// import javax.servlet.ServletException;
// import javax.servlet.ServletRegistration;

// import org.springframework.context.annotation.AnnotationConfigApplicationContext;
// import org.springframework.web.WebApplicationInitializer;
// import org.springframework.web.context.ContextLoaderListener;
// import org.springframework.web.context.WebApplicationContext;
// import org.springframework.web.context.support.GenericWebApplicationContext;
// import org.springframework.web.servlet.DispatcherServlet;

public class WebAppInitializer /* implements WebApplicationInitializer */ {

    // @Override
    // public void onStartup(final ServletContext context) throws ServletException {
    // AnnotationConfigApplicationContext root = new
    // AnnotationConfigApplicationContext();

    // root.scan("com.bonga.web");
    // context.addListener(new ContextLoaderListener((WebApplicationContext) root));

    // ServletRegistration.Dynamic appServlet = context.addServlet("mvc",
    // new DispatcherServlet(new GenericWebApplicationContext()));
    // appServlet.setLoadOnStartup(1);
    // appServlet.addMapping("/");
    // }

}